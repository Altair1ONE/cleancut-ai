import { BLOG_POSTS } from "../../lib/blogPosts";

function escapeXml(str: string) {
  return str
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&apos;");
}

function toRfc822(dateStr: string) {
  // dateStr is YYYY-MM-DD
  const d = new Date(dateStr + "T00:00:00Z");
  return d.toUTCString();
}

export async function GET() {
  const SITE = "https://xevora.org";
  const BASE = "/cleancut";

  const posts = [...BLOG_POSTS].sort((a, b) => (a.date < b.date ? 1 : -1));

  const items = posts
    .map((p) => {
      const link = `${SITE}${BASE}/blog/${p.slug}`;
      const title = escapeXml(p.title);
      const description = escapeXml(p.description);
      const pubDate = toRfc822(p.date);

      return `
        <item>
          <title>${title}</title>
          <link>${link}</link>
          <guid isPermaLink="true">${link}</guid>
          <pubDate>${pubDate}</pubDate>
          <description><![CDATA[${description}]]></description>
        </item>
      `.trim();
    })
    .join("\n");

  const xml = `<?xml version="1.0" encoding="UTF-8" ?>
  <rss version="2.0">
    <channel>
      <title>CleanCut AI Blog</title>
      <link>${SITE}${BASE}/blog</link>
      <description>Guides and use cases for background removal, transparent PNG workflows, product photos, and creator tips.</description>
      <language>en</language>
      ${items}
    </channel>
  </rss>`;

  return new Response(xml, {
    headers: {
      "Content-Type": "application/xml; charset=utf-8",
      "Cache-Control": "public, s-maxage=3600, stale-while-revalidate=86400",
    },
  });
}
